import React from 'react';
import Layout from '../../components/Layout';

const PaymentsList = () => {
    return (
        <Layout>
            <h1>Payments Management</h1>
            <p>List of recent payments and status will go here.</p>
        </Layout>
    );
};

export default PaymentsList;
